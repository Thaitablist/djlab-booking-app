# CLAUDE.md — ระบบร้าน DJ LAB SIAM

ระบบหลังบ้านของร้าน **DJ LAB SIAM** (ตัวแทนจำหน่ายอุปกรณ์ DJ: Pioneer DJ, AlphaTheta, NEO by OYAIDE + โรงเรียนสอน DJ)
เป็นหน้าเว็บล้วน โฮสต์บน GitHub Pages — **push ขึ้น `main` แล้วขึ้นใช้งานจริงทันที** (~1-2 นาที) ไม่มีขั้นตอน build

## หน้าเว็บในระบบ

| ไฟล์ | ใคร | ทำอะไร |
|---|---|---|
| `DJ_LAB_SIAM_BookingApp.html` | พนักงาน | จองห้องซ้อม, ปฏิทิน, จับเวลา, แต้มสะสม, รายงาน — เป็นหน้าแรกและตัวที่ติดตั้งเป็นแอปบนมือถือ (PWA) |
| `stock.html` | พนักงาน | สต็อกเข้า/ออก, รายการสินค้า, ประวัติการเคลื่อนไหว |
| `sales.html` | พนักงาน | ขายหน้าร้าน, ประวัติการขาย, ยกเลิกบิล |
| `customers.html` | พนักงาน | ข้อมูลลูกค้า: ค้นหา/เพิ่ม/แก้ไข/ลบ, ประวัติซื้อ, ประวัติจองห้อง, ชั่วโมงสะสม |
| `activity.html` | เจ้าของ/ผู้ดูแล | บันทึกการใช้งาน: ใครแก้อะไร เมื่อไหร่ ค่าเดิม→ค่าใหม่ (อ่านอย่างเดียว) |
| `daily.html` | พนักงาน | สรุปรายรับ/รายจ่ายประจำวัน — รายรับดึงอัตโนมัติจากการขาย+ค่าเช่าห้อง+ที่กรอกมือ (view `v_daily_income`), รายจ่ายกรอกมือแนบรูปบิลได้, พิมพ์เป็น PDF และโหลด CSV |
| `admin-data.html` | เจ้าของ/ผู้ดูแล | ตรวจและแก้ข้อมูลในตารางโดยไม่ต้องเปิด Supabase — แบ่ง 3 ระดับสิทธิ์ เขียนได้จริงแค่ `products` `customers` `expenses` `income_entries` `booking_settings` ที่เหลืออ่านอย่างเดียว |
| `book.html` | ลูกค้า | ฟอร์มจองห้องซ้อม เปิดผ่าน LINE (LIFF `2010938781-6oxlp5KL`) |
| `member.html` | ลูกค้า | เช็คสิทธิ์สมาชิก + ผูกบัญชี LINE เข้ากับทะเบียน เปิดจากปุ่มล่างซ้ายในเมนู LINE (LIFF `2010938781-IOzFmgO4`) |
| `register.html` | ลูกค้า | สมัครสมาชิกด้วยตัวเอง — ร้านออกรหัสแล้วส่งลิงก์ `?code=25311` ให้ ลูกค้ากรอกข้อมูลเอง เขียนลงชีตแล้วซิงก์เข้าฐานข้อมูล |
| `scan-test.html` | ทดสอบ | หน้าวัดความเร็วการสแกนบาร์โค้ดด้วยกล้อง — **ไม่ใช่หน้าใช้งานจริง** |

ทุกหน้าใช้ Supabase session เดียวกัน (login ครั้งเดียวใช้ได้ทุกหน้า) และมี dropdown `#moduleSelect` บนหัวเว็บไว้สลับระบบ ส่วนแท็บล่าง (`<nav class="nav">`) คือหน้าย่อยของระบบนั้น

## Backend

Supabase (Postgres + Auth + Realtime + RLS) — ไม่มี server ของตัวเอง หน้าเว็บคุยกับ Supabase ตรงๆ
**SQL ทั้งหมด (ตาราง, สิทธิ์, ฟังก์ชัน) ไม่ได้อยู่ใน repo นี้** อยู่ที่ github.com/Thaitablist/djlab-siam-stock-app (private) → `supabase/migrations/` ในเครื่องคือโฟลเดอร์ `DJ LAB SIAM Stock App` ที่นั่นคือแหล่งอ้างอิงจริงของ schema — **ต้องเปิดอ่านก่อนเขียนโค้ดที่แตะฐานข้อมูลทุกครั้ง ห้ามเดาชื่อคอลัมน์**

migration รันด้วยการวางใน Supabase SQL Editor เท่านั้น (เครื่องนี้ไม่มี Supabase CLI และโปรเจกต์ไม่ได้ `supabase init`) ทุกไฟล์เขียนให้รันซ้ำได้โดยไม่ error

**trigger `onEdit` ของ Apps Script ไม่ยิงเมื่อสคริปต์เป็นคนเขียนชีตเอง** — ฟังก์ชันไหนที่เขียนลงชีตด้วยโค้ด
(เช่น `doPost` ของหน้าสมัครสมาชิก) ต้องเรียก `syncRow()` ต่อท้ายเองเสมอ ไม่งั้นข้อมูลค้างอยู่แค่ในชีต
โดยไม่มีอะไรฟ้อง

**ทะเบียนสมาชิก (ตาราง `members`, 807 คน) มีต้นฉบับอยู่ที่ Google Sheet "DJ LAB MEMBERSHIP"** ฐานข้อมูลเป็นสำเนาไว้ค้นหา
แก้ที่ชีตแล้ว Apps Script (`apps-script/SheetSync.gs` ในรีโป schema) ส่งเข้าฐานข้อมูลเองทันทีทีละแถว — ผลบันทึกไว้ในแท็บ `SyncLog` ของชีต
ถ้าแก้ทีเดียวเกิน 50 แถวมันจะไม่ส่ง ให้ใช้ `scripts/import-members.mjs` นำเข้าทั้งไฟล์แทน · **ไม่มีอะไรในแอปเขียนข้อมูลสมาชิกกลับไปที่ชีต**

การยืนยันการจองจะไปกระตุ้นให้บอท LINE ส่งข้อความแจ้งลูกค้าอัตโนมัติ
บอทอยู่คนละ repo: github.com/Thaitablist/djlabsiam-line-bot (private) deploy อัตโนมัติจาก `main` ขึ้น Railway
endpoint ที่รับสัญญาณคือ `/webhooks/booking-confirmed` ใน `index.js`

## กฎที่ห้ามพลาด

1. **ห้ามตั้งชื่อตัวแปร JavaScript ว่า `supabase`** — ชนกับ global ที่ไลบรารีจองไว้ เบราว์เซอร์จะปฏิเสธทั้ง `<script>` แบบเงียบๆ ทุกอย่างตายหมดโดยไม่มี error ให้เห็น ใช้ชื่อ `db` เท่านั้น (เคยทำระบบล่มมาแล้ว 2 ครั้ง)

2. **ต้องทดสอบด้วยเบราว์เซอร์จริงก่อน push ทุกครั้ง** — การตรวจ syntax อย่างเดียวจับบั๊กข้อ 1 ไม่ได้
   ```
   CHROME="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
   "$CHROME" --headless --disable-gpu --no-sandbox --enable-logging=stderr --v=1 \
     --virtual-time-budget=8000 --dump-dom "file://$PWD/ชื่อไฟล์.html" 2>&1 >/dev/null | grep "INFO:CONSOLE"
   ```
   ต้องไม่มี `Uncaught` หรือ `SyntaxError`
   ถ้าอยากทดสอบ**ตรรกะ**ด้วย (ไม่ใช่แค่ syntax) ให้เขียนสคริปต์ที่ก๊อบไฟล์จริงแล้วสลับเฉพาะแท็ก
   `<script src=...supabase...>` เป็น mock + ต่อท้ายด้วยชุดทดสอบ แล้วรัน headless — โค้ดของหน้าเว็บ
   ไม่ต้องแก้อะไรเลย (ตัวอย่างที่ใช้ตรวจ `customers.html` มาแล้ว 39 เคส)

3. **ห้ามใส่ `${JSON.stringify(x)}` ลงใน `onclick="..."`** — `JSON.stringify` ใส่เครื่องหมายคำพูดคู่มาด้วย
   ทำให้ attribute จบกลางคัน กลายเป็น `onclick="fn("` แล้วกดไม่ติดแบบเงียบๆ (เจอมาแล้ว 4 จุด ทั้ง
   แตะสินค้า/แตะลูกค้า/แตะบิล/แตะสินค้าในสต็อก) ใช้ single quote ครอบแทน: `onclick="fn('${x.id}')"`

4. **ห้าม insert/update `sales` หรือ `sale_items` ตรงๆ** — ต้องเรียกผ่านฟังก์ชัน `create_sale` และ `void_sale` เท่านั้น เพราะการขาย 1 บิลต้องบันทึกบิล + รายการ + ตัดสต็อก พร้อมกันในครั้งเดียว ถ้าแยกทำแล้วพลาดกลางทางข้อมูลจะเพี้ยน และยอดเงินคำนวณที่ฐานข้อมูลเสมอ ไม่เชื่อตัวเลขจากเบราว์เซอร์

5. **`stock_movements` เพิ่มได้อย่างเดียว แก้/ลบไม่ได้** — ลงผิดต้องลงรายการปรับยอดกลับ เพื่อให้ตรวจย้อนหลังได้ว่าของหายไปไหน ห้ามทำ UI ที่มีปุ่มแก้หรือลบรายการเคลื่อนไหว

6. **ตัวหนังสือห้ามเล็กกว่า 14px** — เจ้าของร้านกำหนดไว้ ใช้งานบนมือถือทั้งวัน

7. **push ขึ้น GitHub ไม่เท่ากับขึ้นเว็บแล้ว — ต้อง `curl` ยืนยันของจริงทุกครั้ง**
   GitHub Pages เคย build ล้มเหลวเงียบ ๆ 2 commit ติด ของที่ push ไปค้างอยู่บน GitHub แต่ไม่เคยขึ้นเว็บ
   (หน้า `member.html` เสิร์ฟ LIFF ID ตัวอย่างอยู่ครึ่งวันจนปุ่มกดไม่ได้) เช็คสถานะ build ได้ที่
   `gh api repos/Thaitablist/djlab-booking-app/pages/builds/latest`
   มีไฟล์ `.nojekyll` อยู่ที่รากรีโปเพื่อสั่งให้ Pages ข้าม Jekyll — **ห้ามลบ** เว็บนี้เป็น HTML ล้วน
   ไม่ได้ใช้ Jekyll แต่ถ้าปล่อยให้ Jekyll ทำงาน มันจะพังเองโดยไม่บอกสาเหตุที่อ่านรู้เรื่อง

8. **ทุก operation ที่ล้มเหลวต้องแจ้งผู้ใช้ตามจริง** ห้ามขึ้นว่าสำเร็จทั้งที่ error — ทุกหน้ามี `showFatalError()` แสดงข้อผิดพลาดเป็นแถบบนจอ เพราะบนมือถือเปิด console ดูไม่ได้ ถ้าไม่แสดงจะกลายเป็น "กดแล้วไม่มีอะไรเกิดขึ้น"

9. **ข้อความในระบบเป็นภาษาไทย สุภาพ มืออาชีพ** อ้างอิงเฉพาะ Pioneer DJ / AlphaTheta / NEO by OYAIDE — ห้ามเอ่ยถึงแบรนด์คู่แข่ง

10. **บล็อกรับเครื่องยิงบาร์โค้ดมีสองชุด** — อยู่ใน `stock.html` และ `sales.html`
    **แก้ที่หนึ่งต้องแก้อีกที่ด้วย** ไม่แยกเป็นไฟล์ `.js` โดยตั้งใจ เพราะ `sw.js`
    แคชไฟล์ที่ไม่ใช่ HTML แบบ cache-first เครื่องที่เคยโหลดไปแล้วจะค้างของเก่าเงียบ ๆ
    จนกว่าจะขึ้นเลข `CACHE_NAME` ส่วน HTML เป็น network-first แก้แล้วขึ้นทันที
    แก้แล้วต้องรัน `node tests/run-all.mjs` ให้ผ่านทุกข้อ (48 ข้อ)

## ข้อควรรู้เพิ่มเติม

- ค่าเชื่อมต่อ Supabase ที่ฝังในไฟล์ HTML เป็น key สาธารณะโดยการออกแบบ ตัวที่ป้องกันข้อมูลจริงคือกฎสิทธิ์ (RLS) ในฐานข้อมูล — **ห้ามนำ service role key มาใส่ในหน้าเว็บเด็ดขาด**
- พนักงานต้องมีทั้งบัญชี login และแถวในตาราง `admins` ถึงจะใช้งานได้ (มีบัญชีอย่างเดียวจะเข้าได้แต่ไม่เห็นข้อมูลอะไรเลย)
- `sw.js` แคชไฟล์ไว้ ถ้าแก้แล้วไม่ขึ้นให้เพิ่มเลขเวอร์ชันใน `CACHE_NAME`

## กับดักเบอร์โทรจาก Google Sheet

เบอร์ที่พิมพ์เป็นเลขล้วน (ไม่มีขีดคั่น) Sheets จะเก็บเป็น **ตัวเลข** แล้ว **ตัด 0 หน้าทิ้ง** — `0623428555` กลายเป็น `623428555`
ตั้งแต่ในชีต ก่อนที่ระบบเราจะเห็นด้วยซ้ำ ผลคือลูกค้าคนนั้นเช็คสิทธิ์สมาชิกไม่ผ่านตลอดกาลและ**ไม่มี error ให้ใครเห็น**

ป้องกันไว้แล้วด้วยกฎเดียวกันทั้ง 3 ที่ (ฐานข้อมูล `normalize_thai_phone` · `scripts/import-members.mjs` · `customers.html`):
เบอร์ 9 หลักที่ขึ้นต้น 6/8/9 = มือถือที่ 0 หาย เติมกลับให้ (เบอร์บ้าน 9 หลักขึ้นต้นด้วย 0 เสมอ จึงไม่โดนแตะ)
**แก้ที่ต้นทางด้วยได้:** ในชีตเลือกคอลัมน์เบอร์โทร → Format → Number → Plain text

## ซีเรียลรายเครื่อง (`product_units`) — สินค้ารุ่นเดียวกันแต่คนละตัว

กล่องสินค้ามีบาร์โค้ด **2 แบบ** ที่ห้ามสับสนกัน เพราะแก้กลับยาก

| แบบ | อยู่ไหน | เก็บที่ | ซ้ำกันได้ไหม |
|---|---|---|---|
| บาร์โค้ดของรุ่น (EAN-13) | ใต้แท่งบนกล่อง เหมือนกันทุกกล่องของรุ่นนั้น | `products.barcode_ean13` | เหมือนกันทั้งรุ่น |
| ซีเรียลของเครื่อง (SER. NO.) | สติกเกอร์ ไม่ซ้ำกับเครื่องไหนในโลก | `product_units.serial_no` | **ห้ามซ้ำ** (unique ทั้งตาราง) |

**เอาซีเรียลไปผูกเป็น `barcode_ean13` ของรุ่น = พังทั้งรุ่น** เพราะกล่องที่เหลือจะสแกนไม่เจอ
ตอนสแกนไม่เจอ หน้าเว็บจึงถามก่อนเสมอว่ารหัสนี้เป็นแบบไหน ไม่เดาให้

วิธีใช้: หน้า **บันทึกการเคลื่อนไหวสต็อก** → เลือกสินค้า → ประเภท **รับเข้า** → ปุ่ม **📷 ยิงซีเรียลทีละเครื่อง**
ยิงติดกันได้เรื่อย ๆ (กล้องเปิดค้าง อ่านแล้วนับต่อเลย) จำนวนนับให้เองและล็อกไม่ให้พิมพ์ทับ
ของที่ไม่ต้องแยกรายเครื่อง (สาย ฯลฯ) ข้ามช่องนี้แล้วพิมพ์จำนวนได้เหมือนเดิม

ข้อควรรู้:
- ตอนบันทึก **ลง `product_units` ก่อน แล้วค่อยลงยอด `stock_movements`** เพราะซีเรียลซ้ำจะถูกฐานข้อมูลปฏิเสธ
  ถ้าลงยอดก่อนแล้วซีเรียลพัง จะได้ยอดที่ถอนคืนไม่ได้ (กฎข้อ 5: `stock_movements` แก้ไม่ได้)
- เก็บ `barcode_code = serial_no` ด้วย เพื่อให้สแกนซีเรียลแล้วหาสินค้าเจอ (`findByBarcode` มองทั้ง 2 ตาราง)
- ยิงซีเรียลที่มีในระบบแล้ว → บอกว่าเป็นรุ่นไหน สถานะอะไร รับเข้าเมื่อไหร่ ไม่เพิ่มซ้ำ
- เปลี่ยนรุ่นสินค้าหรือเปลี่ยนประเภทเป็นตัดออก/ปรับยอด → ล้างซีเรียลที่ยิงค้างทิ้ง (ไม่งั้นบันทึกผิดรุ่น)
- **ผูกซีเรียลกับการขายแล้ว (migration 022)** — ยิงซีเรียลตอนขาย เครื่องนั้นจะถูกเขียนลง
  `sale_items.product_unit_id` เปลี่ยนสถานะเป็น `sold` และขายซ้ำไม่ได้ · ยกเลิกบิลแล้ว `void_sale`
  คืนสถานะเป็น `in_stock` ให้เอง · จำนวนในตะกร้าถูกล็อกเท่ากับจำนวนซีเรียลที่ยิง
  ⚠️ **ระบบเช็คประกัน/ลงทะเบียนกับมหาจักร เจ้าของสั่งตัดออก ไม่ต้องทำ**

## เครื่องยิงบาร์โค้ดแบบเสียบสาย — ใช้งานจริงแล้ว 31 ส.ค. 2569

ที่ร้านมีเครื่องยิงบาร์โค้ด **TMS `HIDKeyBoard`** (`vid 0x26F1 / pid 0x8801`) เสียบ USB อยู่
เครื่องพวกนี้**ไม่ใช่กล้อง** มันปลอมตัวเป็นคีย์บอร์ดแล้ว "พิมพ์" รหัสที่ยิงได้ลงในช่องที่
โฟกัสอยู่ ตามด้วย Enter — เสียบเครื่องไหนก็ใช้ได้ทันที ไม่ต้องลงไดรเวอร์ ไม่ต้องตั้งค่าอะไร

หน้า `stock.html` และ `sales.html` รับทางนี้แล้วโดยไม่ต้องกดปุ่มเปิดกล้องก่อน
**กล้องยังอยู่ครบ** ปุ่ม 📷 ใช้ได้เหมือนเดิม สองทางวิ่งขนานกัน เปิดกล้องค้างแล้วยิงพร้อมกันก็ได้

### สามอย่างที่ห้ามแก้กลับ

1. **อ่านจาก `event.code` เท่านั้น ห้ามอ่าน `event.key`**
   วัดกับเครื่องจริงตอนแป้นเป็นภาษาไทย: บาร์โค้ด `619659216054` ออกมาเป็น `ุๅตุถต/ๅุจถภ`
   ถ้าอ่านตัวอักษรจะหาสินค้าไม่เจอ และ**พังเฉพาะตอนแป้นเป็นไทย** ซึ่งคือเวลาส่วนใหญ่ที่หน้าร้าน
   ใช้จริง ส่วนคนทดสอบมักสลับเป็นอังกฤษอยู่พอดีแล้วผ่าน กลายเป็นบั๊กที่ไม่มีใครเจอตอนตรวจ

2. **ตัวอักษรคืนเป็นพิมพ์ใหญ่เสมอ ไม่ดู Shift ไม่ดู CapsLock**
   เครื่องที่เผลอเปิด Caps Lock ค้างจะได้ `chmp123354nn` แทน `CHMP123354NN` แล้วหาไม่เจอ
   โดยไม่มีอะไรฟ้องว่าสาเหตุคือปุ่มเดียวบนคีย์บอร์ด — บาร์โค้ดของรุ่นเป็นตัวเลขล้วน
   ส่วนซีเรียล Pioneer/AlphaTheta เป็นตัวพิมพ์ใหญ่ทั้งหมด จึงไม่มีอะไรเสียจากการบังคับ

3. **ไม่ขวางปุ่มล่วงหน้า** — ปล่อยให้ตัวอักษรหล่นลงช่องที่โฟกัสอยู่ตามปกติ (65ms ไม่มีใคร
   ทันเห็น) พอรู้แน่ว่าเป็นการยิงค่อยคืนค่าเดิมทั้งก้อน การพิมพ์ปกติจึงไม่ถูกแตะเลย
   ยกเว้นใน `<select>` ที่ต้องกันตั้งแต่ตัวแรก เพราะการพิมพ์ในนั้นเลื่อนรายการและยิง
   `change` ทันที ซึ่งไปล้างซีเรียลที่ยิงค้างไว้ คืนค่าทีหลังไม่ทัน

### ตัวเลขที่ใช้แยกเครื่องยิงออกจากคนพิมพ์

วัดจากเครื่องจริง: ช่องไฟระหว่างตัวอักษร **เฉลี่ย 6ms สูงสุด 14ms** ปิดท้ายด้วย **Enter**
คนพิมพ์เร็วที่สุดยังห่างกันเกิน 60ms — เส้นที่ตั้งไว้คือ **50ms** และอย่างน้อย **6 ตัวอักษร**
อยากกลับไปเป็นแบบต้องกดปุ่มก่อนค่อยยิง ตั้ง `WEDGE_ENABLED = false` ในทั้งสองไฟล์

### ข้อจำกัดที่ต้องบอกพนักงาน

เครื่องยิงพิมพ์ลงที่ที่โฟกัสอยู่**เสมอ** — คลิกไปหน้าต่างอื่นแล้วยิง รหัสจะไปโผล่ที่นั่น
ไม่ใช่บั๊ก เป็นธรรมชาติของอุปกรณ์ วิธีที่ดีที่สุดคือเปิดหน้าเว็บค้างไว้เป็นหน้าหลักของ
เบราว์เซอร์เครื่องนั้น แล้วคลิกที่หน้าเว็บหนึ่งครั้งก่อนเริ่มยิง

## เทสต์

```bash
node tests/run-all.mjs        # 48 ข้อ ทั้งหน้าสต็อกและหน้าขาย
```

เปิดหน้าเว็บจริงด้วย Chrome headless แล้วสลับเฉพาะแท็ก `<script src>` ของ Supabase เป็น
ตัวปลอม ไฟล์ที่ถูกทดสอบจึงเป็นของจริงทุกบรรทัด ตัวรันร่วมอยู่ที่ `tests/lib/page-test.mjs`
ถ้าแท็ก Supabase ในไฟล์จริงเปลี่ยนไป ตัวรันจะหยุดและฟ้อง ไม่ปล่อยให้เทสต์ไปยิงเน็ตจริงเงียบ ๆ

## งานที่ยังไม่เสร็จ

- **หน้าร้านออนไลน์ djlabsiam.com + ระบบสมาชิกบนเว็บ** — ตัดสินทิศทางแล้ว 15 ส.ค. 69 (ขายออนไลน์จริง ·
  โชว์ราคา · ไม่โชว์จำนวนสต็อกแต่อิงสต็อก · สมาชิกล็อกอินดูข้อมูลตัวเอง · เลิกใช้บัตรสมาชิกแบบกายภาพ)
  **ยังเริ่มไม่ได้ ติด 3 เรื่องที่ต้องตัดสินก่อน** — ดูรายละเอียดใน memory `djlabsiam-storefront-member-portal`
  1. `sync_member_from_sheet` เขียนทับ `full_name/phone/email/address/note/membership/member_type` ทุกครั้ง
     ซึ่งชนกับฟิลด์ที่จะให้สมาชิกแก้เอง — ต้องเลือกว่าจะแยกคอลัมน์ หรือเลิกใช้ชีตเป็นต้นฉบับ
  2. สมาชิกล็อกอินด้วยอะไร (ยังไม่มีระบบล็อกอินสมาชิกเลย) — LINE Login ทำได้เพราะบอทบน Railway
     แลก token ให้ได้ แต่ยังไม่ได้ตัดสิน
  3. ไม่มีตาราง `courses` / `enrollments` / `certificates` เลย และข้อมูลนักเรียนปัจจุบันอยู่ในชีตเป็น
     ข้อความอิสระ — เป็นงานกรอกข้อมูล ไม่ใช่แค่งานโค้ด
- การจองห้อง (`room_bookings`) ยังไม่ผูกกับตาราง `customers` — คอลัมน์ `customer_id` มีอยู่แต่ไม่มีโค้ดไหนเขียนค่าลง
  หน้า `customers.html` จึงต้องจับคู่ประวัติการจองด้วยชื่อ/เบอร์โทรแทน ถ้าจะทำให้แม่นต้องให้พนักงานเลือกลูกค้า
  ตอนยืนยันการจอง แล้วเขียน `customer_id` ลงไป

## เส้นทางแจ้งเตือน LINE — ทดสอบผ่านแล้ว 6 ส.ค. 2569

ลูกค้าจองผ่าน rich menu → พนักงานกดยืนยัน → ข้อความเด้งเข้า LINE ลูกค้า **ครบทั้งเส้น ใช้งานได้จริง**
แปลว่า trigger `trg_notify_booking_confirmed` ติดตั้งอยู่ในฐานข้อมูลแล้ว และ secret ในนั้นตรงกับ
`BOOKING_WEBHOOK_SECRET` บน Railway (ไฟล์ `004` ในรีโปยังเป็น placeholder อยู่ตามตั้งใจ — secret ตัวจริง
ไม่เก็บในรีโป ถ้าต้องรัน `004` ใหม่ต้องไปเอาค่าจริงจาก Railway มาแทนที่ก่อน)

### ถ้าวันหลังข้อความไม่เด้ง — ไล่ตามลำดับ หยุดที่ข้อแรกที่ไม่ผ่าน

1. **จองจากมือถือจริง** เปิด `book.html` **จากในแอป LINE** (ผ่าน rich menu) เท่านั้น — เปิดจาก Safari/Chrome
   จะไม่มี LINE userId และระบบจะบันทึกเป็น `source: 'online_web'` ซึ่งไม่มีทางส่งข้อความได้
2. **เปิดหน้าจองของพนักงาน** แตะการจองนั้น ถ้าขึ้นแถบเขียว "ระบบจะส่งข้อความแจ้งลูกค้าทาง LINE อัตโนมัติ"
   แปลว่าเก็บ `line_user_id` ได้แล้ว ถ้าขึ้นแถบเหลืองแปลว่าข้อ 1 ยังไม่ผ่าน
3. **กดยืนยันการจอง** แล้วดูว่าข้อความเด้งเข้า LINE ของลูกค้าหรือไม่ (ปกติภายในไม่กี่วินาที)
4. **ถ้าไม่เด้ง** ให้ไล่จากปลายทางกลับมา:
   - Railway → Logs ของบอท: ถ้าเห็น request เข้ามาแต่ error แสดงว่าเป็นเรื่องของ LINE token/userId
   - ถ้า **ไม่เห็น request เข้าเลย** = trigger ไม่ยิง หรือยิงแล้วโดน 401 → ตรวจในฐานข้อมูล:
     ```sql
     select * from net._http_response order by created desc limit 5;  -- 401 = secret ไม่ตรง
     select tgname from pg_trigger where tgname = 'trg_notify_booking_confirmed';  -- ไม่มีแถว = ยังไม่ได้ติดตั้ง
     ```
   - แก้ secret ให้ตรงกัน: รัน `004_booking_notify_trigger.sql` ใหม่โดยแทนที่ `__BOOKING_WEBHOOK_SECRET__`
     ด้วยค่าเดียวกับ `BOOKING_WEBHOOK_SECRET` ใน Railway
5. **ผู้รับต้องแอด LINE OA ของร้านเป็นเพื่อนก่อน** ไม่งั้น LINE ตอบ 403 บอทส่งไม่ได้
   (ทอดนี้ไม่มีอะไรในระบบฟ้อง เพราะบอทตอบ 200 ให้ฐานข้อมูลไปแล้วก่อนจะยิงหา LINE)
