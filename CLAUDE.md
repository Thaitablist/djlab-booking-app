# CLAUDE.md — ระบบร้าน DJ LAB SIAM

ระบบหลังบ้านของร้าน **DJ LAB SIAM** (ตัวแทนจำหน่ายอุปกรณ์ DJ: Pioneer DJ, AlphaTheta, NEO by OYAIDE + โรงเรียนสอน DJ)
เป็นหน้าเว็บล้วน โฮสต์บน GitHub Pages — **push ขึ้น `main` แล้วขึ้นใช้งานจริงทันที** (~1-2 นาที) ไม่มีขั้นตอน build

## หน้าเว็บในระบบ

| ไฟล์ | ใคร | ทำอะไร |
|---|---|---|
| `DJ_LAB_SIAM_BookingApp.html` | พนักงาน | จองห้องซ้อม, ปฏิทิน, จับเวลา, แต้มสะสม, รายงาน — เป็นหน้าแรกและตัวที่ติดตั้งเป็นแอปบนมือถือ (PWA) |
| `stock.html` | พนักงาน | สต็อกเข้า/ออก, รายการสินค้า, ประวัติการเคลื่อนไหว |
| `sales.html` | พนักงาน | ขายหน้าร้าน, ประวัติการขาย, ยกเลิกบิล |
| `customers.html` | พนักงาน | ข้อมูลลูกค้า: ค้นหา/เพิ่ม/แก้ไข/ลบ, ประวัติซื้อ, ประวัติจองห้อง, ชั่วโมงสะสม |
| `activity.html` | เจ้าของ/ผู้ดูแล | บันทึกการใช้งาน: ใครแก้อะไร เมื่อไหร่ ค่าเดิม→ค่าใหม่ (อ่านอย่างเดียว) |
| `book.html` | ลูกค้า | ฟอร์มจองห้องซ้อม เปิดผ่าน LINE (LIFF) |
| `scan-test.html` | ทดสอบ | หน้าวัดความเร็วการสแกนบาร์โค้ดด้วยกล้อง — **ไม่ใช่หน้าใช้งานจริง** |

ทุกหน้าใช้ Supabase session เดียวกัน (login ครั้งเดียวใช้ได้ทุกหน้า) และมี dropdown `#moduleSelect` บนหัวเว็บไว้สลับระบบ ส่วนแท็บล่าง (`<nav class="nav">`) คือหน้าย่อยของระบบนั้น

## Backend

Supabase (Postgres + Auth + Realtime + RLS) — ไม่มี server ของตัวเอง หน้าเว็บคุยกับ Supabase ตรงๆ
**SQL ทั้งหมด (ตาราง, สิทธิ์, ฟังก์ชัน) ไม่ได้อยู่ใน repo นี้** อยู่ที่ github.com/Thaitablist/djlab-siam-stock-app (private) → `supabase/migrations/` ในเครื่องคือโฟลเดอร์ `DJ LAB SIAM Stock App` ที่นั่นคือแหล่งอ้างอิงจริงของ schema — **ต้องเปิดอ่านก่อนเขียนโค้ดที่แตะฐานข้อมูลทุกครั้ง ห้ามเดาชื่อคอลัมน์**

migration รันด้วยการวางใน Supabase SQL Editor เท่านั้น (เครื่องนี้ไม่มี Supabase CLI และโปรเจกต์ไม่ได้ `supabase init`) ทุกไฟล์เขียนให้รันซ้ำได้โดยไม่ error

การยืนยันการจองจะไปกระตุ้นให้บอท LINE ส่งข้อความแจ้งลูกค้าอัตโนมัติ
บอทอยู่คนละ repo: github.com/Thaitablist/djlabsiam-line-bot (private) deploy อัตโนมัติจาก `main` ขึ้น Railway
endpoint ที่รับสัญญาณคือ `/webhooks/booking-confirmed` ใน `index.js`

## กฎที่ห้ามพลาด

1. **ห้ามตั้งชื่อตัวแปร JavaScript ว่า `supabase`** — ชนกับ global ที่ไลบรารีจองไว้ เบราว์เซอร์จะปฏิเสธทั้ง `<script>` แบบเงียบๆ ทุกอย่างตายหมดโดยไม่มี error ให้เห็น ใช้ชื่อ `db` เท่านั้น (เคยทำระบบล่มมาแล้ว 2 ครั้ง)

2. **ต้องทดสอบด้วยเบราว์เซอร์จริงก่อน push ทุกครั้ง** — การตรวจ syntax อย่างเดียวจับบั๊กข้อ 1 ไม่ได้
   ```
   CHROME="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
   "$CHROME" --headless --disable-gpu --no-sandbox --enable-logging=stderr --v=1 \
     --virtual-time-budget=8000 --dump-dom "file://$PWD/ชื่อไฟล์.html" 2>&1 >/dev/null | grep "INFO:CONSOLE"
   ```
   ต้องไม่มี `Uncaught` หรือ `SyntaxError`
   ถ้าอยากทดสอบ**ตรรกะ**ด้วย (ไม่ใช่แค่ syntax) ให้เขียนสคริปต์ที่ก๊อบไฟล์จริงแล้วสลับเฉพาะแท็ก
   `<script src=...supabase...>` เป็น mock + ต่อท้ายด้วยชุดทดสอบ แล้วรัน headless — โค้ดของหน้าเว็บ
   ไม่ต้องแก้อะไรเลย (ตัวอย่างที่ใช้ตรวจ `customers.html` มาแล้ว 39 เคส)

3. **ห้ามใส่ `${JSON.stringify(x)}` ลงใน `onclick="..."`** — `JSON.stringify` ใส่เครื่องหมายคำพูดคู่มาด้วย
   ทำให้ attribute จบกลางคัน กลายเป็น `onclick="fn("` แล้วกดไม่ติดแบบเงียบๆ (เจอมาแล้ว 4 จุด ทั้ง
   แตะสินค้า/แตะลูกค้า/แตะบิล/แตะสินค้าในสต็อก) ใช้ single quote ครอบแทน: `onclick="fn('${x.id}')"`

4. **ห้าม insert/update `sales` หรือ `sale_items` ตรงๆ** — ต้องเรียกผ่านฟังก์ชัน `create_sale` และ `void_sale` เท่านั้น เพราะการขาย 1 บิลต้องบันทึกบิล + รายการ + ตัดสต็อก พร้อมกันในครั้งเดียว ถ้าแยกทำแล้วพลาดกลางทางข้อมูลจะเพี้ยน และยอดเงินคำนวณที่ฐานข้อมูลเสมอ ไม่เชื่อตัวเลขจากเบราว์เซอร์

5. **`stock_movements` เพิ่มได้อย่างเดียว แก้/ลบไม่ได้** — ลงผิดต้องลงรายการปรับยอดกลับ เพื่อให้ตรวจย้อนหลังได้ว่าของหายไปไหน ห้ามทำ UI ที่มีปุ่มแก้หรือลบรายการเคลื่อนไหว

6. **ตัวหนังสือห้ามเล็กกว่า 14px** — เจ้าของร้านกำหนดไว้ ใช้งานบนมือถือทั้งวัน

7. **ทุก operation ที่ล้มเหลวต้องแจ้งผู้ใช้ตามจริง** ห้ามขึ้นว่าสำเร็จทั้งที่ error — ทุกหน้ามี `showFatalError()` แสดงข้อผิดพลาดเป็นแถบบนจอ เพราะบนมือถือเปิด console ดูไม่ได้ ถ้าไม่แสดงจะกลายเป็น "กดแล้วไม่มีอะไรเกิดขึ้น"

8. **ข้อความในระบบเป็นภาษาไทย สุภาพ มืออาชีพ** อ้างอิงเฉพาะ Pioneer DJ / AlphaTheta / NEO by OYAIDE — ห้ามเอ่ยถึงแบรนด์คู่แข่ง

## ข้อควรรู้เพิ่มเติม

- ค่าเชื่อมต่อ Supabase ที่ฝังในไฟล์ HTML เป็น key สาธารณะโดยการออกแบบ ตัวที่ป้องกันข้อมูลจริงคือกฎสิทธิ์ (RLS) ในฐานข้อมูล — **ห้ามนำ service role key มาใส่ในหน้าเว็บเด็ดขาด**
- พนักงานต้องมีทั้งบัญชี login และแถวในตาราง `admins` ถึงจะใช้งานได้ (มีบัญชีอย่างเดียวจะเข้าได้แต่ไม่เห็นข้อมูลอะไรเลย)
- `sw.js` แคชไฟล์ไว้ ถ้าแก้แล้วไม่ขึ้นให้เพิ่มเลขเวอร์ชันใน `CACHE_NAME`

## งานที่ยังไม่เสร็จ

- ระบบสแกนบาร์โค้ดด้วยกล้องยังไม่ได้ทดสอบกับกล่องสินค้าจริงหลังแก้รอบล่าสุด
- การจองห้อง (`room_bookings`) ยังไม่ผูกกับตาราง `customers` — คอลัมน์ `customer_id` มีอยู่แต่ไม่มีโค้ดไหนเขียนค่าลง
  หน้า `customers.html` จึงต้องจับคู่ประวัติการจองด้วยชื่อ/เบอร์โทรแทน ถ้าจะทำให้แม่นต้องให้พนักงานเลือกลูกค้า
  ตอนยืนยันการจอง แล้วเขียน `customer_id` ลงไป

## เส้นทางแจ้งเตือน LINE — ทดสอบผ่านแล้ว 6 ส.ค. 2569

ลูกค้าจองผ่าน rich menu → พนักงานกดยืนยัน → ข้อความเด้งเข้า LINE ลูกค้า **ครบทั้งเส้น ใช้งานได้จริง**
แปลว่า trigger `trg_notify_booking_confirmed` ติดตั้งอยู่ในฐานข้อมูลแล้ว และ secret ในนั้นตรงกับ
`BOOKING_WEBHOOK_SECRET` บน Railway (ไฟล์ `004` ในรีโปยังเป็น placeholder อยู่ตามตั้งใจ — secret ตัวจริง
ไม่เก็บในรีโป ถ้าต้องรัน `004` ใหม่ต้องไปเอาค่าจริงจาก Railway มาแทนที่ก่อน)

### ถ้าวันหลังข้อความไม่เด้ง — ไล่ตามลำดับ หยุดที่ข้อแรกที่ไม่ผ่าน

1. **จองจากมือถือจริง** เปิด `book.html` **จากในแอป LINE** (ผ่าน rich menu) เท่านั้น — เปิดจาก Safari/Chrome
   จะไม่มี LINE userId และระบบจะบันทึกเป็น `source: 'online_web'` ซึ่งไม่มีทางส่งข้อความได้
2. **เปิดหน้าจองของพนักงาน** แตะการจองนั้น ถ้าขึ้นแถบเขียว "ระบบจะส่งข้อความแจ้งลูกค้าทาง LINE อัตโนมัติ"
   แปลว่าเก็บ `line_user_id` ได้แล้ว ถ้าขึ้นแถบเหลืองแปลว่าข้อ 1 ยังไม่ผ่าน
3. **กดยืนยันการจอง** แล้วดูว่าข้อความเด้งเข้า LINE ของลูกค้าหรือไม่ (ปกติภายในไม่กี่วินาที)
4. **ถ้าไม่เด้ง** ให้ไล่จากปลายทางกลับมา:
   - Railway → Logs ของบอท: ถ้าเห็น request เข้ามาแต่ error แสดงว่าเป็นเรื่องของ LINE token/userId
   - ถ้า **ไม่เห็น request เข้าเลย** = trigger ไม่ยิง หรือยิงแล้วโดน 401 → ตรวจในฐานข้อมูล:
     ```sql
     select * from net._http_response order by created desc limit 5;  -- 401 = secret ไม่ตรง
     select tgname from pg_trigger where tgname = 'trg_notify_booking_confirmed';  -- ไม่มีแถว = ยังไม่ได้ติดตั้ง
     ```
   - แก้ secret ให้ตรงกัน: รัน `004_booking_notify_trigger.sql` ใหม่โดยแทนที่ `__BOOKING_WEBHOOK_SECRET__`
     ด้วยค่าเดียวกับ `BOOKING_WEBHOOK_SECRET` ใน Railway
5. **ผู้รับต้องแอด LINE OA ของร้านเป็นเพื่อนก่อน** ไม่งั้น LINE ตอบ 403 บอทส่งไม่ได้
   (ทอดนี้ไม่มีอะไรในระบบฟ้อง เพราะบอทตอบ 200 ให้ฐานข้อมูลไปแล้วก่อนจะยิงหา LINE)
